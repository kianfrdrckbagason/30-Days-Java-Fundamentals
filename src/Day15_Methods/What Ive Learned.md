Today, I learned about methods, including how to create methods with parameters and use the return keyword to return a value from the method. I learned that the return value must match the method's declared return type, such as using int when the method returns an integer. I also learned how to pass arguments to a method and call the method directly inside System.out.println() to display its returned result.